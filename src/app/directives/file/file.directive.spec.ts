import { FileDirective } from './file.directive';

describe('FileDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDirective();
    expect(directive).toBeTruthy();
  });
});
